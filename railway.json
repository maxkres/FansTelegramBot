{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install --user -r requirements.txt"
  },
  "deploy": {
    "startCommand": "uvicorn server.app.main:app --host 0.0.0.0 --port 8080"
  }
}